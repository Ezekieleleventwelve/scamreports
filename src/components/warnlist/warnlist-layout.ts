/** Shared shell width — must match Header + Footer (`max-w-6xl px-4 sm:px-6`). */
export const SHELL_WIDTH = "mx-auto w-full max-w-6xl px-4 sm:px-6";

/** Warnlist pages: same horizontal line as header/footer. */
export const WARNLIST_PAGE = `${SHELL_WIDTH} py-6 sm:py-8`;
